[pytest]
pythonpath = backend .
testpaths = backend/tests
asyncio_mode = auto
markers =
    unit: Unit tests
    security: Security and authentication boundary tests
    robustness: Acoustic degradation and robustness tests
    benchmark: Offline benchmark evaluation tests
filterwarnings =
    ignore::DeprecationWarning
